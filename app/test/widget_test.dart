// Basic test file - tests can be expanded later
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Basic test', (WidgetTester tester) async {
    expect(1 + 1, equals(2));
  });
}
